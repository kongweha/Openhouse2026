{
  "name": "openhouse2026",
  "version": "1.0.0",
  "private": true,
  "description": "Static Firebase stamp-card and registration system for Open House 2026.",
  "scripts": {
    "check": "node scripts/validate-static-site.mjs && node --test scripts/test-firebase-service.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
