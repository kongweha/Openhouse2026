{
  "name": "openhouse2026",
  "version": "1.0.0",
  "private": true,
  "description": "Open House 2026 registration, stamp-card, and admin system.",
  "scripts": {
    "check": "node scripts/validate-static-site.mjs && node --check functions/src/index.js && node --test functions/test/domain.test.js",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
